<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Update page</title>
        <link rel="stylesheet" href="/stylesheets/style.css">
        <script src="https://cdn.tailwindcss.com"></script>
    </head>

    <body>
        <div class="w-full h-full bg-zinc-900 p-10 text-white">
            <div class="bav mb-10">
                <a class="text-blue-500" href="/read">View All Users</a>
            </div>
            <h1 class="text-3xl tracking-tighter mb-4">Update User</h1>
            <form action="/update/<%= user._id %>" method="post">
                <input type="text" placeholder="Enter Your Name"
                    class="px-5 py-2 outline-none bg-zinc-700 rounded-lg border-2 border-zinc-700" name="name"
                    value="<%= user.name %>">
                <input type="text" placeholder="Enter Email"
                    class="px-5 py-2 outline-none bg-zinc-700 rounded-lg border-2 border-zinc-700" name="email"
                    value="<%= user.email %>">
                <input type="text" placeholder="Enter Image URL"
                    class="px-5 py-2 outline-none bg-zinc-700 rounded-lg border-2 border-zinc-700" name="image"
                    value="<%= user.image %>">
                <input type="submit" value="Update User" class="px-5 py-2 rounded-lg bg-green-600 ml-1">
            </form>
        </div>
    </body>

</html>