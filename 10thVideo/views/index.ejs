<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Auth</title>
        <script src="https://cdn.tailwindcss.com"></script>
    </head>

    <body>
        <div class="w-full h-screen bg-zinc-900 text-white p-10">
            <h2 class="text-3xl mb-10">Create user</h2>
            <form action="/create" method="post">
                <input type="text" class="px-3 py-2 rounded-lg bg-transparent border-2 border-zinc-800 outline-none"
                    placeholder="username" name="username">
                <input type="email" class="px-3 py-2 rounded-lg bg-transparent border-2 border-zinc-800 outline-none"
                    placeholder="email" name="email">
                <input type="password" class="px-3 py-2 rounded-lg bg-transparent border-2 border-zinc-800 outline-none"
                    placeholder="password" name="password">
                <input type="number" class="px-3 py-2 rounded-lg bg-transparent border-2 border-zinc-800 outline-none"
                    placeholder="age" name="age">
                <input type="submit" class="px-5 py-2 rounded-lg bg-blue-500" value="Create user">
            </form>
        </div>
    </body>

</html>