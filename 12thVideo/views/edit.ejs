<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Profile</title>
        <script src="https://cdn.tailwindcss.com"></script>
    </head>

    <body>
        <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
            <div class="flex justify-end w-full">
                <a class="bg-red-500 rounded-lg px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
            </div>
            <h5 class="mb-5">Edit Your Post</h5>

            <form action="/update/<%= post._id %>" method="post">
                <textarea name="content" placeholder="Write anything your mind says!"
                    class="w-1/3 mt-3 px-3 py-3 rounded-lg h-40 resize-none bg-transparent block outline-none border-2 border-zinc-600"><%= post.content %></textarea>
                <input type="submit" value="Edit post" class="block bg-yellow-700 px-3 py-2 mt-2 rounded-lg">
            </form>


        </div>
    </body>

</html>